#include "local-include/reg.h"
#include <cpu/cpu.h>
#include <cpu/ifetch.h>
#include <cpu/decode.h>

#define R(i) gpr(i)
#define Mr vaddr_read
#define Mw vaddr_write

enum {
  TYPE_I, TYPE_U, TYPE_S,TYPE_J,TYPE_Jr,TYPE_B,TYPE_R,TYPE_Is,
  TYPE_N, // none
};

#define src1R(n) do { *src1 = R(n); } while (0)
#define src2R(n) do { *src2 = R(n); } while (0)
#define destR(n) do { *dest = n; } while (0)
#define src1I(i) do { *src1 = i; } while (0)
#define src2I(i) do { *src2 = i; } while (0)
#define destI(i) do { *dest = i; } while (0)

static word_t immI(uint32_t i) { return SEXT(BITS(i, 31, 20), 12); }
static word_t immIs(uint32_t i) { return SEXT(BITS(i, 25, 20), 6); }
static word_t immU(uint32_t i) { return SEXT(BITS(i, 31, 12), 20) << 12; }
static word_t immS(uint32_t i) { return (SEXT(BITS(i, 31, 25), 7) << 5) | BITS(i, 11, 7); }
static word_t immJ(uint32_t i) { return (SEXT(BITS(i, 31, 31), 1) << 20) | (BITS(i, 19, 12) << 12) | (BITS(i, 20, 20) << 11) | (BITS(i, 30, 21) << 1); }
static word_t immJr(uint32_t i) { return (SEXT(BITS(i, 31, 20), 12));  }
static word_t immB(uint32_t i) { return (SEXT(BITS(i, 31, 31), 1) << 12) | (BITS(i, 7, 7) << 11) | (BITS(i, 30, 25) << 5) | (BITS(i, 11, 8) << 1); }

static void decode_operand(Decode *s, word_t *dest, word_t *src1, word_t *src2, int type) {
  uint32_t i = s->isa.inst.val;
  int rd  = BITS(i, 11, 7);
  int rs1 = BITS(i, 19, 15);
  int rs2 = BITS(i, 24, 20);
  destR(rd);
  switch (type) {
    case TYPE_I: src1R(rs1);     src2I(immI(i)); break;
    case TYPE_Is: src1R(rs1);     src2I(immIs(i)); break;
    case TYPE_R: src1R(rs1);     src2R(rs2); break;
    case TYPE_U: src1I(immU(i)); break;
    case TYPE_S: destI(immS(i)); src1R(rs1); src2R(rs2); break;
    case TYPE_J: src2I(immJ(i)); break;
    case TYPE_Jr:src1R(rs1); src2I(immJr(i)); break;
    case TYPE_B: src1R(rs1); src2R(rs2); destI(immB(i)); break;
  }
}

static int decode_exec(Decode *s) {
  word_t dest = 0, src1 = 0, src2 = 0;
  s->dnpc = s->snpc;

#define INSTPAT_INST(s) ((s)->isa.inst.val)
#define INSTPAT_MATCH(s, name, type, ... /* body */ ) { \
  decode_operand(s, &dest, &src1, &src2, concat(TYPE_, type)); \
  __VA_ARGS__ ; \
}

  INSTPAT_START();
  INSTPAT("??????? ????? ????? ??? ????? 00101 11", auipc  , U, R(dest) = src1 + s->pc);
  INSTPAT("?????????? ?????????? ????? 0110111",    lui	   , U, R(dest) = src1 & 0xfffffffffffff000);
  
  INSTPAT("??????? ????? ????? 011 ????? 00000 11", ld     , I, R(dest) = Mr(src1 + src2, 8));
  INSTPAT("???????????? ????? 100 ????? 0000011",   lbu    , I, R(dest) = (uint64_t)Mr(src1 + src2, 1));  
  INSTPAT("???????????? ????? 101 ????? 0000011",   lhu    , I, R(dest) = (uint64_t)Mr(src1 + src2, 2)); 
  INSTPAT("???????????? ????? 010 ????? 0000011",   lw     , I, R(dest) = SEXT(Mr(src1 + src2, 4),32));  
  INSTPAT("???????????? ????? 001 ????? 0000011",   lh     , I, R(dest) = SEXT(Mr(src1 + src2, 2),16)); 
  INSTPAT("??????? ????? ????? 000 ????? 0010011",  addi   , I, R(dest) = src1 + src2);  
  INSTPAT("??????? ????? ????? 111 ????? 0010011",  andi   , I, R(dest) = src1 & src2);  
  INSTPAT("??????? ????? ????? 100 ????? 0010011",  xori   , I, R(dest) = src1 ^ src2); 
  INSTPAT("000000 ?????? ????? 001 ????? 0010011",  slli   , I, R(dest) = src1 << BITS(src2, 5, 0) );  
  INSTPAT("000000 ?????? ????? 101 ????? 0010011",  srli   , I, R(dest) = (uint64_t)src1 >> src2 );  
  INSTPAT("???????????? ????? 011 ????? 0010011",   sltiu  , I, R(dest) = ((uint64_t)src1 < (uint64_t)src2)? 1 : 0 ); 
  INSTPAT("???????????? ????? 000 ????? 0011011",   addiw  , I, R(dest) = SEXT(BITS(src1 + src2, 31, 0),32)); 
  INSTPAT("0000000 ????? ????? 001 ????? 0011011",  slliw  , I, R(dest) = SEXT(BITS(src1 << src2, 31, 0),32)); 
  INSTPAT("0100000 ????? ????? 101 ????? 0011011",  sraiw  , I, R(dest) = (int64_t)SEXT(BITS(src1, 31, 0), 32) >> BITS(src2, 4, 0)); 
  INSTPAT("0000000 ????? ????? 101 ????? 0011011",  srliw  , I, R(dest) = (int64_t)SEXT((uint64_t)BITS(src1, 31, 0) >> src2, 32)); 
   
  
  INSTPAT("010000 ?????? ????? 101 ????? 0010011",  srai   , Is, R(dest) = src1 >> src2);  
  
 
  INSTPAT("0000000 ????? ????? 000 ????? 0110011",  add    , R, R(dest) = src1 + src2); 
  INSTPAT("0000000 ????? ????? 111 ????? 0110011",  and    , R, R(dest) = src1 & src2); 
  INSTPAT("0100000 ????? ????? 000 ????? 0110011",  sub    , R, R(dest) = src1 - src2);
  INSTPAT("0000000 ????? ????? 110 ????? 0110011",  or     , R, R(dest) = src1 | src2);
  INSTPAT("0000001 ????? ????? 000 ????? 0110011",  mul    , R, R(dest) = src1 * src2); 
  INSTPAT("0000000 ????? ????? 000 ????? 0111011",  addw   , R, R(dest) = SEXT(BITS(src1 + src2, 31, 0),32));  
  INSTPAT("0100000 ????? ????? 000 ????? 0111011",  subw   , R, R(dest) = SEXT(BITS(src1 - src2, 31, 0),32)); 
  INSTPAT("0000001 ????? ????? 000 ????? 0111011",  mulw   , R, R(dest) = SEXT(BITS(src1 * src2, 31, 0),32)); 
  INSTPAT("0000001 ????? ????? 100 ????? 0111011",  divw   , R, R(dest) = SEXT(BITS(src1 / src2, 31, 0),32)); 
  INSTPAT("0000001 ????? ????? 110 ????? 0111011",  remw   , R, R(dest) = BITS(BITS(src1, 31, 0) % BITS(src2, 31, 0), 31, 0));
  INSTPAT("0000000 ????? ????? 001 ????? 0111011",  sllw   , R, R(dest) = SEXT(BITS(src1, 31, 0), 32) << BITS(src2, 4, 0));  
  INSTPAT("0000000 ????? ????? 011 ????? 0110011",  sltu   , R, R(dest) = ((uint64_t)src1 < (uint64_t)src2) ? 1 : 0 ); 
  INSTPAT("0000000 ????? ????? 010 ????? 0110011",  slt    , R, R(dest) = ((int64_t)src1 < (int64_t)src2) ? 1 : 0 ); 
  INSTPAT("0100000 ????? ????? 101 ????? 0111011",  sraw   , R, R(dest) = (int64_t)SEXT(BITS(src1, 31, 0), 32) >> BITS(src2, 4, 0)); 
  INSTPAT("0000000 ????? ????? 101 ????? 0111011",  srlw   , R, R(dest) = (int64_t)SEXT((uint64_t)BITS(src1, 31, 0) >> BITS(src2, 4, 0), 32)); 
  
  
  INSTPAT("??????? ????? ????? 011 ????? 0100011",  sd     , S, Mw(src1 + dest, 8, src2));
  INSTPAT("??????? ????? ????? 000 ????? 0100011",  sb     , S, Mw(src1 + dest, 1, BITS(src2, 7, 0)));
  INSTPAT("??????? ????? ????? 010 ????? 0100011",  sw     , S, Mw(src1 + dest, 4, BITS(src2, 31, 0)));
  INSTPAT("??????? ????? ????? 001 ????? 0100011",  sh     , S, Mw(src1 + dest, 2, BITS(src2, 15, 0)));


  INSTPAT("?????????? ?????????? ????? 1101111",    jal	   , J, {R(dest) = s->pc + 4 ; s->dnpc = s->pc + src2; } );
  INSTPAT("???????????? ????? 000 ????? 1100111",   jalr   , Jr,{R(dest) = s->pc + 4 ; s->dnpc = ((src1+src2) & 0xfffffffe); } );

  INSTPAT("??????? ????? ????? 000 ????? 1100011",  beq    , B, {if((int64_t)src1 == (int64_t)src2) s->dnpc = s->pc + dest;} );
  INSTPAT("??????? ????? ????? 001 ????? 1100011",  bne    , B, {if((int64_t)src1 != (int64_t)src2) s->dnpc = s->pc + dest;} );
  INSTPAT("??????? ????? ????? 101 ????? 1100011",  bge    , B, {if((int64_t)src1 >= (int64_t)src2) s->dnpc = s->pc + dest;} );
  INSTPAT("??????? ????? ????? 100 ????? 1100011",  blt    , B, {if((int64_t)src1 < (int64_t)src2) s->dnpc = s->pc + dest;} );
  INSTPAT("??????? ????? ????? 110 ????? 1100011",  bltu   , B, {if((uint64_t)src1 < (uint64_t)src2) s->dnpc = s->pc + dest;} );


  INSTPAT("0000000 00001 00000 000 00000 11100 11", ebreak , N, NEMUTRAP(s->pc, R(10))); // R(10) is $a0
  INSTPAT("??????? ????? ????? ??? ????? ????? ??", inv    , N, INV(s->pc));
  INSTPAT_END();

  R(0) = 0; // reset $zero to 0

  return 0;
}

int isa_exec_once(Decode *s) {
  s->isa.inst.val = inst_fetch(&s->snpc, 4);
  return decode_exec(s);
}
